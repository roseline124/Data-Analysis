inspect(sort(rioter.transaction)[1:10])
inspect(sort(rules)[1:10])
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.4, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$판매액)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
mq <- read_excel("momq.xlsx")
mq <- mq[1:3000,]
str(mq)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$주문번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
View(mq)
rioter.list
rioter.list <- split(mq$대표아이월령, mq$상위브랜드)
rioter.list
head(rioter.list)
rioter.list <- split(mq$대표아이월령, mq$상위브랜드)
head(rioter.list)
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
head(rioter.list)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.4, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.2, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
rule.list
inspect(sort(rules)[1:10])
mq <- read_excel("momq.xlsx")
length(mq)
n.rows(mq)
str(mq)
nrow(mq)
library(readxl)
mq <- read_excel("momq_2018.xlsx")
mq <- read_excel("momq_2018.xlsx")
library(readxl)
mq <- read_excel("momq_2018.xlsx")
mq <- read_excel("momq_2018.xlsx")
mq <- read.csv("momq_2018.csv")
mq <- read_excel("momq_2018.xlsx")
mq <- read_excel("momq_2018.xlsx", rowIndex = 5:700)
mq <- read_excel("momq.xlsx")
# mq <- read_excel("momq.xlsx")
mq <- read.csv("momq_2018.csv")
# mq <- read_excel("momq.xlsx")
mq <- read.csv("momq_2018.csv")
library(readxl)
# mq <- read_excel("momq.xlsx")
mq <- read.csv("momq_2018.csv")
mq <- read_excel("momq_2018_50000.xlsx")
View(mq)
library(arules)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$주문번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rioter.list
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.4, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.6, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
summary(rules)
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
inspect(sort(rules)[1:10])
# 상위브랜드 연관성 분석
rioter.list <- split(mq$브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.4, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
summary(rules)
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
rule.list
names(mq)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.6, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
summary(rules)
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
rule.list
inspect(sort(rules)[1:10])
unique(mq$카테고리)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상품명, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.2, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules <- apriori(rioter.transaction, parameter=list(support=0.3, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.4, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.33, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.35, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.35, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
summary(rules)
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
inspect(sort(rules)[1:10])
inspect(sort(rules)[1:10])
inspect(sort(rules)[1:10])
inspect(sort(rules)[1:10])
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.35, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
plot(rules)
plot(rules)
install.packages("arulesViz")
# install.packages("arulesViz")
library(arulesViz)
plot(rules)
plot(rules,method="graph")
plot(rules,method="graph", interactive = T)
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.4, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
# install.packages("arulesViz")
library(arulesViz)
plot(rules,method="graph", interactive = T)
rules <- apriori(rioter.transaction, parameter=list(support=0.6, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.55, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
plot(rules,method="graph", interactive = T)
plot(rules,method="graph")
plot(rules,method="graph", interactive = TRUE)
plot(rules,method="graph")
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.55, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
plot(rules,method="graph")
plot(rules,method="graph", interactive = T)
plot(rules,method="graph", engine='interactive')
plot(rules,method="graph", engine='interactive')
plot(rules,method="paracoord")
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.55, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
plot(rules,method="paracoord")
data("Groceries")
Groceries
View(Groceries)
head(Groceries)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.55, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$주문번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
names(mq)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
image(rioter.transaction)
image(rioter.transaction)
rioter.list
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.9, minlen=1, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=1, smax=1), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=1, smax=1), control=list(verbos=F))
rules
summary(rules)
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
names(mq$회원번호)
class(mq$회원번호)
mq$회원번호 <- as.character(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
class(mq$회원번호)
class(mq$상위브랜드)
class(mq$대표아이월령)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
unique(mq$카테고리)
inspect(sort(rules)[1:10])
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
head(rioter.list)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
head(rules)
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
head(rules)
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2, smax=1), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, confidence=0.8, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(minlen=2))
rules <- apriori(rioter.list, parameter=list(minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(minlen=2), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
mq <- read_excel("momq_2018.xlsx")
mq <- read_excel("momq_2018.xlsx")
library(readxl)
mq <- read_excel("momq_2018.xlsx")
library(arules)
mq$회원번호 <- as.character(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.7, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.6, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.3, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.2, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.1, minlen=2), control=list(verbos=F))
rules
mq$회원번호 <- as.character(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.1, minlen=2), control=list(verbos=F))
rules
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.6, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.5, minlen=2), control=list(verbos=F))
rules
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
# 상위브랜드 연관성 분석
rioter.list <- split(mq$상위브랜드, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
rule.list <- as.data.frame(inspect(rules))
rule.list <- rule.list[order(rule.list$lift, decreasing=TRUE), ]
rule.list
rule.list
inspect(sort(rules)[1:10])
inspect(sort(rules)[1:10])
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$대표아이월령)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
plot(rules,method="graph", engine='interactive')
# install.packages("arulesViz")
library(arulesViz)
plot(rules,method="graph", engine='interactive')
plot(rules,method="graph")
plot(rules,method="paracoord")
mq$회원번호 <- as.character(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
head(rioter.list)
mq$회원번호 <- as.factor(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
unique(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, as.character(mq$회원번호))
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
rioter.transaction
install.packages("discretize")
discretize(mq$회원번호)
mq$회원번호 <- as.character(mq$회원번호)
mq$회원번호 <- as.character(mq$회원번호)
library(readxl)
mq <- read_excel("momq_2018.xlsx")
library(arules)
mq$회원번호 <- as.character(mq$회원번호)
discretize(mq$회원번호)
mq$회원번호 <- as.numeric(mq$회원번호)
mq$회원번호 <- discretize(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
head(rioter.list)
rules <- apriori(rioter.transaction, parameter=list(support=0.4, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.6, minlen=2), control=list(verbos=F))
rules
rules <- apriori(rioter.transaction, parameter=list(support=0.9, minlen=2), control=list(verbos=F))
rules
mq$회원번호 <- as.numeric(mq$회원번호)
mq$회원번호 <- discretize(mq$회원번호, 3000)
mq$회원번호 <- discretize(mq$회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
head(rioter.list)
mq$회원번호 <- as.numeric(mq$회원번호)
head(mq$회원번호)
mq <- read_excel("momq_2018.xlsx")
mq$bin.회원번호 <- discretize(mq$회원번호)
head(mq$bin.회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
head(rioter.list)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$bin.회원번호)
head(rioter.list)
mq$bin.회원번호 <- discretize(mq$회원번호, 3000)
mq$bin.회원번호 <- discretize(mq$회원번호, categories=3)
mq$bin.회원번호 <- discretize(mq$회원번호, categories=3000)
mq$bin.회원번호 <- discretize(mq$회원번호, categories=10000)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$bin.회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
mq$bin.회원번호 <- discretize(mq$회원번호, categories=1000)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$bin.회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
nrows(unique(mq$회원번호))
nrow(unique(mq$회원번호))
ncol(unique(mq$회원번호))
length(unique(mq$회원번호))
length((mq$회원번호))
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$bin.회원번호)
# 상위브랜드 연관성 분석
rioter.list <- split(mq$카테고리, mq$회원번호)
rioter.transaction <- as(rioter.list, "transactions")
rioter.transaction
rules <- apriori(rioter.transaction, parameter=list(support=0.1, minlen=2), control=list(verbos=F))
rules
